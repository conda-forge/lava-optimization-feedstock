{% set name = "lava-optimization" %}
{% set version = "0.2.4" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/lava-nc/lava-optimization/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 186db96b40e6f185e0ec036bd24ddb4844779d1a29040fcc19cdf5422fe80717

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6,<3.11
    - poetry
  run:
    - python >=3.6,<3.11
    - numpy
    - lava
    - pandas
    - networkx
    - schema
    - scikit-optimize
    - scipy

test:
  source_files:
    - tests
  imports:
    - lava.lib.optimization.problems.problems
    - lava.lib.optimization.solvers
    - lava.lib.optimization.solvers.qp.solver
  commands:
    - ipython kernel install --name "python3" --user
    - python -m unittest
  requires:
    - asteval
    - nbformat
    - nbconvert
    - ipython
    - ipykernel

about:
  home: https://github.com/lava-nc/lava-optimization
  summary: A library of solvers that leverage neuromorphic hardware for constrained optimization.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Tobias-Fischer
    - mgkwill
